{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn app:app --timeout 0 --workers 2 --worker-class gthread --threads 4",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
